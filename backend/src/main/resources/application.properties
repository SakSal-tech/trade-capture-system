# Server Configuration
server.port=8080
server.servlet.context-path=/

# H2 Database Configuration - File-based with proper settings
spring.datasource.url=jdbc:h2:file:./data/tradingdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# H2 Console (for development)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.defer-datasource-initialization=true

# Initialize with data.sql
## Database initialization
# Do NOT run the SQL seed against the developer file-backed H2 DB by default.
# The seed (data.sql) is large and not idempotent; re-applying it against a
# persistent file-based database can cause unique-key violations and abort
# Spring's ApplicationContext refresh. For local development keep the
# persistent DB unchanged and only apply the seed in the test profile.
spring.sql.init.mode=never
spring.sql.init.data-locations=classpath:data.sql

# Jackson Configuration for JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# Logging Configuration
logging.level.com.technicalchallenge=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# CORS Configuration
management.endpoints.web.cors.allowed-origins=http://localhost:3000,http://localhost:5173
management.endpoints.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
management.endpoints.web.cors.allowed-headers=*

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,env,beans,configprops,loggers,httptrace,mappings
management.endpoint.health.show-details=always
management.endpoint.health.show-components=always
management.endpoint.metrics.enabled=true
management.endpoint.info.enabled=true
management.info.env.enabled=true
management.info.build.enabled=true
management.info.git.mode=full
management.info.java.enabled=true
management.info.os.enabled=true

# OpenAPI/Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.packages-to-scan=com.technicalchallenge.controller
springdoc.paths-to-match=/api/**
